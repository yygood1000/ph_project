<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 正中心的dialog/popup的动画，透明度渐变显示、退出-->
    <style name="center_dialog_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit_anim</item>
    </style>

    <!-- 底部的dialog/popup的动画，从下上升显示-->
    <style name="bottom_dialog_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/slide_in_bottom</item>
        <item name="android:windowExitAnimation">@anim/slide_out_bottom</item>
    </style>

    <!-- 底部的dialog/popup的动画，右上角缩小退出 版本更新框-->
    <style name="right_top_out_dialog_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/slide_in_bottom</item>
        <item name="android:windowExitAnimation">@anim/zoom_right_top_exit</item>
    </style>

    <!-- 底部的dialog的动画，顶部退出 版本更新框-->
    <style name="top_out_dialog_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/slide_in_bottom</item>
        <item name="android:windowExitAnimation">@anim/slide_out_top</item>
    </style>

    <!-- pop 底部弹出动画 -->
    <style name="mypopwindow_anim_style">
        <item name="android:windowEnterAnimation">@anim/popshow_anim</item>
        <!-- 指定显示的动画xml -->
        <item name="android:windowExitAnimation">@anim/pophidden_anim</item>
        <!-- 指定消失的动画xml -->
    </style>
</resources>